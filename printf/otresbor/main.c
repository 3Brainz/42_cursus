#include "libprintf.h"
#include <stdio.h>
/*%[flags][width][.precision][length]specifier*/
int main()
{
   // t_flags flags;

    ////flags = new_flag();
	//flag_cleaner(&flags);
	int a = ft_printf("%%%%");
}